# components/ui/

Чисті переюзовні UI-примітиви без бізнес-логіки: Button, Input, Checkbox, Badge.
Компоненти з логікою конкретної фічі — у `features/<feature>/components/`.
Layout-компоненти (Header, Footer) — у `components/layout/`.
