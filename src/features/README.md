# features/

Доменна логіка по фічах: cart, favorites, auth, catalog.
Кожна фіча тримає в собі свої компоненти, store, хуки, схеми форм, типи.

Приклад:

```
features/
  cart/
    components/
    hooks/
    store.ts
    types.ts
```
